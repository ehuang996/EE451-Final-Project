# Results Layout

This directory is organized by CARC run:

- [run1/](run1/) — original pure-C++ sweep on CARC (job `3272373`)
- [run2/](run2/) — BLAS-backed KNN/MLP rerun on CARC (job `3278100`)

Per-run folders contain the report markdown, CSV outputs, and compiled binaries
for that run.
